# dsh-coding-sidebar 1.0.1

## 修复

- **设置页插件名徽标去除上游品牌前缀**：「侧边卡片」设置分区头部的名称徽标误写为 `DSH-coding-sidebar`（fork 时沿用了上游 `DSH-` 大写品牌前缀），现显示为与 npm 包名一致的 `dsh-coding-sidebar`。版本徽标逻辑不变，仍由构建期从 package.json 注入的 `SIDEBAR_SERVICE_VERSION` 驱动（本版起显示 `v1.0.1`）。

## 升级

- 与 1.0.0 完全兼容，无迁移、无契约变化。
- KCoder 侧：预置声明 `^1.0.0` 语义化范围直接满足，bundle 物化按版本差自动覆盖。
