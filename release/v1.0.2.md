# dsh-coding-sidebar 1.0.2

> 任务管理页条目过多时默认折叠历史：子代理仅展开最新 5 条、后台任务最新 3 条，
> 可一键展开。

## 变更

- `SubagentView`：名册与后台任务列表按「最新 N 条」默认折叠（子代理 5 条 /
  后台任务 3 条），折叠区可一键展开——条目多时当前关注项不再被历史挤出视口；
- 新增折叠区样式（`SubagentView.module.css`）；
- 20 个语言包各补 4 个文案键，`locales.ts` 同步；
- README 更新该交互说明。

## 兼容性说明

- 纯客户端展示层改动，RPC 形状与宿主契约不变；
- 折叠只是默认态而非能力限制：展开后行为与 1.0.1 一致。

## 验证

真源提交 `9d9aa0a`（功能）、`66adb90`（入版 + 产物重建，即 npm 发布树）。
补录说明：本文件于 v1.0.31 发版时按仓库惯例回溯补齐；该版提交未单列验证输出。